checkpoint:
  model_dir: saved_model

model:
  max_seq_length: 512
  load_in_4bit: true

data:
  test_csv: sample_data/test.csv
  text_column: text
  label_column: label

test:
  batch_size: 8
  max_new_tokens: 24
  temperature: 0.0
  do_sample: false
  prompt_template: "Tin nhan: {text}\nY dinh:"
